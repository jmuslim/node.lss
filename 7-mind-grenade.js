const num1 =5;
const num2 = 10;

function addValues(){
    console.log(`the sun is ${num1 + num2}`)
}

addValues() // This call invoke.
// Insted of of export I invoke. 
// when we invoke we can execute the function. 
// Check some more details function invoking instate of export. how dose it work and pros and corns.